# Install with the setup script for your OS: setup_venv.bat (Windows) or ./setup_venv.sh (Linux)
# Python 3.10-3.14 (pythonnet's range; 3.13 recommended). tkinter is not installed via
# pip: it ships with the Windows Python installer; on Debian/Ubuntu: sudo apt install python3-tk
# The browser is Windows' own WebView2 runtime; setup_venv.bat fetches the SDK it is hosted with.

# Embedded browser: hosts the WebView2 SDK (.NET) in the Tk window
pythonnet>=3.0.3,<4

# Browser automation
selenium>=4.6

# Database and HTTP
pymongo
requests
# Loads MONGODB_URI from the .env file (see .env.example)
python-dotenv

# Windows only: clicking in the Infinity POS window
pyautogui; sys_platform == "win32"
pygetwindow; sys_platform == "win32"
